- name: variables from files
  host: localhost #where you install the ansible server
  become: yes
  vars_files: 
  - variables.yaml
  tasks: 
  - name: printing varibles from varibles file
    ansible.builtin.debug:
      msg: "{{NAME}} IS  FROM {{DISTRICT}} IN {{STATE}}"
      